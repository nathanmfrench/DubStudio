{
  "name": "dubstudio-lambda-functions",
  "version": "1.0.0",
  "description": "Lambda functions for DubStudio",
  "main": "dist/videos/upload.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.1531.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}