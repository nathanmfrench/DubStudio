{
  "name": "dubstudio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "infrastructure"
  ],
  "scripts": {
    "start:frontend": "cd frontend && expo start",
    "build:eas": "cd frontend && eas build --profile production",
    "deploy:infra": "cd infrastructure && npm run deploy"
  },
  "devDependencies": {
    "eas-cli": "^8.1.0"
  }
}